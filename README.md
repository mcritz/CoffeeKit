# CoffeeKit

A package supporting [CoffeeCoffeeCoffeeCoffee](https://CoffeeCoffeeCoffee.coffee) web service and clients.

This package exists to share types between server and client code in a predictable way.

Notably, it include `Protocols` and `*Public` types that can be used to assert properties and functionality shared between server and client.
